language: python
python:
  - "2.6"
  - "2.7"

install: "pip install coverage"
script:
  - "coverage run -a --branch oblig02/PreTeX/prepro.py oblig02/tests/e2e/testfiles/tex_before.xtex /dev/null -q"
  - "coverage run -a --branch --source=oblig02/PreTeX `which py.test`"

notifications:
  email:
    recipients: nicolai.skogheim@gmail.com
    on_success: change # default: change
    on_failure: change # default: always
